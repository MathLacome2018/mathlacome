Compact uppercase label for categories, tags, and article metadata.

```jsx
<Badge variant="solid">Science</Badge>
<Badge variant="outline">Analysis</Badge>
<Badge variant="muted">Draft</Badge>
```

Notable variants: `solid` (black fill), `outline` (border only), `muted` (subtle gray). Sizes: `sm`, `md`, `lg`.
